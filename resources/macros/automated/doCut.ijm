setTool(17);
setForegroundColor(0, 0, 0);


/*
waitForUser("Click okay when finished");

if(selectionType() != 7){
    print(selectionType());
    return;
}

setForegroundColor(0, 0, 0);
getSelectionCoordinates(xCoordinates, yCoordinates);
print(lengthOf(xCoordinates));
print(lengthOf(yCoordinates));

setLineWidth(3);
drawLine(xCoordinates[0], yCoordinates[0], xCoordinates[1], yCoordinates[1]);


run("Select None");
setForegroundColor(255, 255, 255);
*/